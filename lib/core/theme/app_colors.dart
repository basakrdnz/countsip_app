import 'package:flutter/material.dart';

class AppColors {
  // Primary - Earthy Brown
  static const primary = Color(0xFF674739);
  static const primaryLight = Color(0xFF8E6351);
  static const primaryDark = Color(0xFF53362A);
  static const brandDark = Color(0xFF4B3126);

  // Backgrounds - Dark Theme
  static const background = Color(0xFF0A0A0A);
  static const surface = Color(0xFF1A1A1A);
  static const surfaceElevated = Color(0xFF242424);

  // Light Theme Backgrounds (for reference, but opting for dark default)
  static const backgroundLight = Color(0xFFF5F5F5);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceElevatedLight = Color(0xFFFAFAFA);

  // Text - High Contrast
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textTertiary = Color(0xFF6B6B6B);

  // Text Light Mode
  static const textPrimaryLight = Color(0xFF4B3126);
  static const textSecondaryLight = Color(0xFF714A39);
  static const textTertiaryLight = Color(0xFF8E6351);

  // Borders - Subtle
  static const border = Color(0xFF2A2A2A);
  static const borderLight = Color(0xFFE0E0E0);

  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFA726);
  static const error = Color(0xFFEF5350);

  // Shadows
  static const shadow = Color(0x40000000);
  static const shadowLight = Color(0x1A000000);
}
